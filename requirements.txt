# Core dependencies
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.0.0

# Audio processing
librosa>=0.10.0
scipy>=1.11.0

# Video processing
opencv-python>=4.8.0
imageio>=2.33.0
# imageio-ffmpeg is installed via system package (apt-get) in Colab
# This avoids Python 3.12 compatibility issues

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# Deep learning (placeholder for actual model loading)
torch>=2.0.0
torchvision>=0.15.0

# Development and testing
pytest>=7.4.0
black>=23.0.0
pylint>=3.0.0

# NOTE: TTS and ffmpeg are installed separately in Colab
# See EXECUTION_STEPS.md Cell 2 for installation
